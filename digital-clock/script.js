const zones=[{id:'Asia/Kolkata',city:'New Delhi',label:'India Standard Time'},{id:'America/New_York',city:'New York',label:'Eastern Time'},{id:'Europe/London',city:'London',label:'United Kingdom'},{id:'Asia/Tokyo',city:'Tokyo',label:'Japan Standard Time'},{id:'Australia/Sydney',city:'Sydney',label:'Australian Eastern Time'},{id:'America/Los_Angeles',city:'Los Angeles',label:'Pacific Time'},{id:'Europe/Paris',city:'Paris',label:'Central European Time'},{id:'Asia/Dubai',city:'Dubai',label:'Gulf Standard Time'}];let selected=['Asia/Kolkata','America/New_York','Europe/London','Asia/Tokyo'];let is24Hour=true;const $=id=>document.getElementById(id),getZone=id=>zones.find(z=>z.id===id),formatTime=(d,z)=>new Intl.DateTimeFormat('en-US',{timeZone:z,hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:!is24Hour}).format(d),formatDate=(d,z)=>new Intl.DateTimeFormat('en-US',{timeZone:z,weekday:'long',month:'short',day:'numeric',year:'numeric'}).format(d),offset=(d,z)=>new Intl.DateTimeFormat('en-US',{timeZone:z,timeZoneName:'short'}).formatToParts(d).find(p=>p.type==='timeZoneName')?.value||'';function renderCards(now){$('clockGrid').innerHTML=selected.map(id=>{const z=getZone(id);return `<article class="clock-card"><div class="card-top"><div><h2 class="city">${z.city}</h2><span class="zone-label">${z.label}</span></div><button class="remove" data-remove="${id}" aria-label="Remove ${z.city}">×</button></div><div class="card-time">${formatTime(now,id)}</div><div class="card-date">${formatDate(now,id)} <span class="offset">· ${offset(now,id)}</span></div></article>`}).join('');document.querySelectorAll('[data-remove]').forEach(b=>b.addEventListener('click',()=>{selected=selected.filter(id=>id!==b.dataset.remove);renderCards(new Date())}))}function update(){const now=new Date(),z=Intl.DateTimeFormat().resolvedOptions().timeZone;$('localTime').textContent=formatTime(now,z);$('localDate').textContent=formatDate(now,z);$('localZone').textContent=z.replaceAll('_',' ');$('lastUpdated').textContent=`Last sync ${now.toLocaleTimeString([], {hour:'2-digit',minute:'2-digit',second:'2-digit'})}`;renderCards(now)}function renderOptions(){$('zoneOptions').innerHTML=zones.filter(z=>!selected.includes(z.id)).map(z=>`<button class="zone-option" type="button" data-add="${z.id}"><strong>${z.city}</strong><br><small>${z.label}</small></button>`).join('')||'<p class="zone-label">All available locations are already displayed.</p>';document.querySelectorAll('[data-add]').forEach(b=>b.addEventListener('click',()=>{selected.push(b.dataset.add);$('zoneDialog').close();update()}))}$('formatToggle').addEventListener('click',()=>{is24Hour=!is24Hour;$('formatToggle').textContent=is24Hour?'24H':'12H';update()});$('themeToggle').addEventListener('click',()=>{const dark=document.documentElement.dataset.theme==='dark';document.documentElement.dataset.theme=dark?'light':'dark';$('themeToggle').textContent=dark?'☼':'☾'});$('addZone').addEventListener('click',()=>{renderOptions();$('zoneDialog').showModal()});update();setInterval(update,1000);
